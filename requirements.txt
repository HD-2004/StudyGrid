# Pinned to versions with prebuilt wheels for Python 3.14 (cp314).
# Older pins force a source build of pydantic-core, which needs the MSVC
# linker and fails on a machine without Visual Studio build tools.
fastapi==0.141.1
uvicorn==0.53.0
pydantic==2.13.5
python-dotenv==1.2.3
httpx==0.28.1
openai==3.14.1
python-multipart==0.0.32
pypdf==6.19.0
python-docx==1.2.0
