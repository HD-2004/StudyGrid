{
  "name": "studygrid",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:web": "npm --prefix web run dev:vite --",
    "build": "npm --prefix web run build",
    "check": "npm --prefix web run check",
    "preview": "npm --prefix web run preview --"
  }
}
